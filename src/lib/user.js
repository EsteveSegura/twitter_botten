class User{
    constructor(screenName, name, userId, avatar, history){
        this.screenName = screenName,
        this.name = name, 
        this.userId = userId,
        this.avatar = avatar,
        this.history = history
    }
}

module.exports = User;